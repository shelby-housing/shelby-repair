source("renv/activate.R")

get_rehab_filepath <- function(file = "rehab_cases") {
  fp <- Sys.getenv("LEAD_REHAB_FOLDER")
  
  if (file == "rehab_cases") {
    paste0(fp, "Rehab Cases by Program Year.xlsx")
  }
}
